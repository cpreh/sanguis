fcppt_utils_load_source_files(client/files.txt SANGUIS_CLIENT_FILES)

sanguis_generate_symbol_header(client)

sanguis_add_library(
  client
  "${SANGUIS_CLIENT_FILES}"
  DEPS
  ${sanguis_collision_TARGET}
  ${sanguis_common_TARGET}
  ${sanguis_core_TARGET}
  ${sanguis_creator_TARGET}
  ${sanguis_model_TARGET}
  ${sanguis_server_TARGET}
  ${sanguis_tiles_TARGET}
  ${sgeaudio_TARGET}
  ${sgecharconv_TARGET}
  ${sgeconfig_TARGET}
  ${sgeconsole_TARGET}
  ${sgeconsolegfx_TARGET}
  ${sgecore_TARGET}
  ${sgecursor_TARGET}
  ${sgefont_TARGET}
  ${sgefontdraw_TARGET}
  ${sgegui_TARGET}
  ${sgeimage2d_TARGET}
  ${sgeimagecolor_TARGET}
  ${sgeinput_TARGET}
  ${sgelog_TARGET}
  ${sgeparse_TARGET}
  ${sgeparseini_TARGET}
  ${sgeparsejson_TARGET}
  ${sgerenderer_TARGET}
  ${sgerucksack_TARGET}
  ${sgesprite_TARGET}
  ${sgesystems_TARGET}
  ${sgetimer_TARGET}
  ${sgetexture_TARGET}
  ${sgeviewport_TARGET}
  ${sgewindow_TARGET}
  ${alda_TARGET}
  ${alda_net_TARGET}
  ${awl_TARGET}
  fcppt_boost_interface
  ${fcppt_core_TARGET}
  ${fcppt_filesystem_TARGET}
  ${fcppt_log_TARGET}
  ${fcppt_options_TARGET}
  fcppt_parse_interface
  ${ASIO_LIBS}
  TRANSITIVE_DEPS
  sanguis_server_interface
  sgeconsolegfx_interface
  sgerenderer_interface
  alda_interface
  alda_net_interface
  fcppt_core_interface
  fcppt_log_interface
  fcppt_options_interface
  COMPILE_FLAGS
  ${ASIO_OPTIONS})
